syntax = "proto3";

package indicator;

service Shepherd {
  rpc Update(Request) returns (Response) {}
}

message Request {
    string id = 1;
    string label = 2;
    string icon = 3;
    bool active = 4;
}

message Response {
    string err = 1;
}
